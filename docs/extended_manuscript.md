# Extended manuscript: the alternative-paths layer

**Updated:** {{DATE}}
**Role:** one section per alternative approach to the target — organised by
route, not by chronology. The master ([`../master.tex`](../master.tex)) is the
main line; this document holds everything else that is real.

Rules of this file (from `AGENTS.md`, "Write proofs out in full"):

- Every path is written out **with complete proofs**, step-checked. The status
  `proved (note only)` — a statement with a reference where the argument
  should be — is not an acceptable entry.
- An input carried as an explicit hypothesis rather than reproved is fine, but
  the section must say so where the hypothesis is used.
- A result that is simply the master's own line waiting for authorisation is
  an **integration item**, not an alternative path — mark it as such.
- Check every path against
  [`../REFUTED_CONJECTURES.md`](../REFUTED_CONJECTURES.md) before it gets a
  section, and record the grade of anything it revives.

---

## Route 1: {{name}}

**Status:** {{open / partially proved / integration item}}
**What it has proved.** {{Full statements with complete proofs.}}
**What it still needs.** {{The precise missing statements.}}
**Where it rejoins the spine.** {{Which master theorem it would replace or
feed.}}
